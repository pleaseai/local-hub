pre-commit:
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt -- --check
    clippy:
      glob: "*.rs"
      run: cargo clippy -- -D warnings

commit-msg:
  commands:
    commitlint:
      run: bunx --no -- commitlint --edit {1}
